# Video script (≤ 3 min)

0:00 Sheet on the table, phone in hand. "Ringmeter measures a ring from one photo over this printed kit."
0:15 Coin on the scale-check circle: "Scale check — the coin fits, so the print is 100 %."
0:30 Ring into the zone; phone overhead; tap Take photo.
0:45 Result: overlay, 17.xx ± 0.3 mm, EU/US/UK chips, checks list; open "Tap to result" → engine init once, then ms per stage; cost line.
1:20 Retake case: tilt the phone 40° → TILT card with the hint. Second retake: shoot without the marker → NO_MARKER.
1:50 Laptop: `testset/RESULTS.md` — per-photo error table, max error, the unsupported photos rejected for the right reason.
2:20 Repo: `npm test` green, `ground-truth.json` committed before photos (git log). Limits section on the page.
2:50 "Measured, not promised. Thanks."
